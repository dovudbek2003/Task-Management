const roles = {
    ADMIN: "admin",
    MANAGER: "manager",
    WORKER: "worker",
    SUPERADMIN: "superAdmin"
};

const status = {
    PROCESS: "process",
    DONE: "done",
    TOOK: "took"
};

module.exports = { roles, status }